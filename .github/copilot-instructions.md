# Copilot instructions

Read [`.agents/agents.md`](../.agents/agents.md) before changing this repository. Run tests with `scripts/run_tests.sh`, not raw `pytest`. Use Conventional Commits (`type(AIS-<nr>): summary`).
